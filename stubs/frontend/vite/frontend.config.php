<?php

/** stack + profile — entry, entries, manifest, dev.* are filled automatically. Use vite_tags() in Twig. */
return [
    'profile' => 'spa',
    'stack' => 'vite',
    // 'entries' => ['src/main.js'],
    // 'refresh' => false,
];
